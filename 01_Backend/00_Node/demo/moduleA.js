// here we execute the code in moduleB
//  and retrieve the data from the module.exports of moduleB.js
const moduleB = require("./moduleB.js");

// we take the data from moduleB and display it in the terminal
console.log(moduleB);
